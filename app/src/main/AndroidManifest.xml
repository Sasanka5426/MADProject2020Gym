<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.madproject2020gym">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".updayePackage">

        </activity>
        <activity android:name=".packages_admin">

        </activity>
        <activity android:name=".update_fee_admin"></activity>
        <activity android:name=".gym_admin">

        </activity>
        <activity android:name=".admin_profile" />
        <activity android:name=".add_new_package_admin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".add_new_member_admin">

        </activity>
        <activity android:name=".admin_home" >

        </activity>
    </application>

</manifest>